rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    match /tickets/{ticketId} {
      // Allow read/list if user is authenticated
      allow read, list: if request.auth != null;
      allow write: if request.auth != null;
    }

  }
}