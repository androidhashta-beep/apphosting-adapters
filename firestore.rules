rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allow public read and write access for all documents and collections.
      // This is appropriate for this public-facing application and is intended to
      // permanently resolve the persistent permission errors.
      allow read, write: if true;
    }
  }
}
