rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allow public read access for all documents and collections
      allow read: if true;
      
      // Only allow writes if the user is authenticated
      allow write: if request.auth != null;
    }
  }
}
